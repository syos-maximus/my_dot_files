[core]
	pager = delta
[interactive]
	diffFilter = delta --color-only
[delta] # need to install git-delta for this to work
    navigate = true
    side-by-side = true
    line-numbers = true
[merge]
	conflictStyle = zdiff3
[user]
	email = sebastian@aerolaw.org
	name = syos-maximus
